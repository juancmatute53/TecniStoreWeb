import { ClientesrolPipe } from './clientesrol.pipe';

describe('ClientesrolPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientesrolPipe();
    expect(pipe).toBeTruthy();
  });
});
