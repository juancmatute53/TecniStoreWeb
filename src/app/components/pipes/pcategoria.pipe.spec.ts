import { PcategoriaPipe } from './pcategoria.pipe';

describe('PcategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new PcategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
