import { Sqldetallespedidos } from './sqldetallespedidos';

describe('Sqldetallespedidos', () => {
  it('should create an instance', () => {
    expect(new Sqldetallespedidos()).toBeTruthy();
  });
});
